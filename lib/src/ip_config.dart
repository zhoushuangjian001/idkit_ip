// Frequently used character codes.
/// The host url of IPv4.
const String host = 'api.ipify.org';

/// The host url of IPv6.
const String v6Host = 'api64.ipify.org';

/// Internet Address Request Protocol.
const String scheme = 'https';

/// Network request response text form
enum IPFormat {
  text,
  json,
}
